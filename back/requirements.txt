# Database
pgdbtoolkit

# API
fastapi==0.104.1
uvicorn==0.24.0

# Pydantic - Versiones compatibles
pydantic==2.7.4
pydantic-settings==2.7.1
email-validator>=2.1.0

# JWT
python-jose[cryptography]==3.3.0
PyJWT==2.8.0

# OAuth2 y archivos
python-multipart==0.0.12

# Password hashing
bcrypt==4.0.1
passlib[bcrypt]==1.7.4

# Dotenv - Versión compatible con pgdbtoolkit
python-dotenv==1.0.1

# IA y OpenAI (solo reconocimiento de plantas, sin generación de imágenes)
openai>=1.35.3

# Email service
sendgrid==6.11.0

# Google Auth
google-auth==2.36.0
requests>=2.31.0

# Supabase Storage para almacenamiento de imágenes
supabase>=2.0.0

# Pillow para procesamiento de imágenes
Pillow

# Dateutil para parsing de fechas
python-dateutil

# Redis para cache
redis[hiredis]>=5.0.0

# Polars para agregaciones rápidas (10x más rápido que pandas)
polars>=0.19.0

# ============================================
# Load Testing (solo para desarrollo)
# ============================================
locust>=2.20.0
faker>=22.0.0
tqdm>=4.66.0